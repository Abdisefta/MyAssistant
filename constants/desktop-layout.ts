/** Breakpoint (px) where sidebar layout replaces bottom tabs. */
export const DESKTOP_BREAKPOINT = 900;

/** Max width for main content panel on wide screens. */
export const DESKTOP_MAX_CONTENT_WIDTH = 1200;

/** Fixed width of the left navigation sidebar. */
export const DESKTOP_SIDEBAR_WIDTH = 220;
